// @flow
export type SliderState = {
    currentSlide: number,
    totalSlides: number,
    innerWidth: number,
    isPrevDisabled: boolean,
    isNextDisabled: boolean,
    isSlidingDisabled: boolean
};
